package stevemerollis.codetrial.weather.network

import stevemerollis.codetrial.weather.BaseTest
import org.junit.jupiter.api.Test

class NetworkHelperImplTest: BaseTest() {

//    @MockK private val papiApi: IPapiApi = mockk()
//    @MockK private val netStateUtil: NetStateUtil = mockk()
//    val helper: NetworkHelperImpl = NetworkHelperImpl(papiApi, netStateUtil)

//    @Test
//    suspend fun `test when the Moshi library gets resources from json, it produces parsed results`() {

//        val resultFlow: Flow<NetworkResult<PayMethodsResponse>> = helper.paymentMethodsGET()
//
//        resultFlow.collect {
//            when (it) {
//                is NetworkResult.Success -> assertNotNull(it)
//                else -> fail()
//            }
//            val results: NetworkResult.Success<PayMethodsResponse> = it as NetworkResult.Success<PayMethodsResponse>
//            val userInfo: PayMethodsResponse = results.data as PayMethodsResponse
//
//        }

 //   }

}